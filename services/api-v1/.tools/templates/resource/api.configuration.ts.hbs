import { IApiConfig } from "src/core/controller.layer";

export const config: IApiConfig = {
  availableFields: ["id", "name"],

  defaultLimit: 25,
  maxLimit: 50,

  availableIndexes: [],

  availableFilters: {
    name: {
      type: "string",
      availableOperations: ["$eq"],
    },
  },
};
