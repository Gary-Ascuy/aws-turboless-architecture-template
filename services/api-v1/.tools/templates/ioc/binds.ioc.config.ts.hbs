container.bind<I{{className name}}Service>(types.I{{className name}}Service).to({{className name}}Service);
container.bind<I{{className name}}Repository>(types.I{{className name}}Repository).to({{className name}}Repository);
